# Sanity — public IDs, safe to commit values for the frontend.
# Copy this file to .env.local and fill in.

VITE_SANITY_PROJECT_ID=8jqyl8cp
VITE_SANITY_DATASET=production

# Used by the studio config (sanity.config.ts). Optional — falls back to the
# values hardcoded in the file.
SANITY_STUDIO_PROJECT_ID=8jqyl8cp
SANITY_STUDIO_DATASET=production
